on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v3

      - name: Build APK
        uses: pavi2410/Webview-to-APK@v1.2
        with:
          title: "Our SMM"
          url: "https://oursmm.top"
